---
// @ts-ignore
import { Icon } from "astro-icon/components";
---

<section class="py-16 lg:py-24 relative overflow-hidden">
  <!-- Animated background elements with emojis -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 left-10 w-32 h-32 bg-blue-100 rounded-full opacity-20 animate-float"></div>
    <div class="absolute bottom-20 right-16 w-24 h-24 bg-purple-100 rounded-full opacity-30 animate-float-delayed"></div>
    <div class="absolute top-1/2 left-1/4 w-16 h-16 bg-green-100 rounded-full opacity-25 animate-float-slow"></div>
    
    <!-- Floating emojis -->
    <div class="absolute top-32 right-20 text-4xl animate-float opacity-30">🚀</div>
    <div class="absolute bottom-32 left-20 text-3xl animate-float-delayed opacity-25">💡</div>
    <div class="absolute top-1/3 right-1/4 text-2xl animate-float-slow opacity-20">⭐</div>
    <div class="absolute bottom-1/3 left-1/3 text-3xl animate-float opacity-15">✨</div>
    <div class="absolute top-1/4 left-16 text-2xl animate-float-delayed opacity-20">🎯</div>
    <div class="absolute bottom-1/4 right-12 text-2xl animate-float-slow opacity-25">🎨</div>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    
    <!-- Technologies Section -->
    <div class="mt-24 text-center mb-16 animate-fade-in-up">
      <h2 class="text-center text-2xl text-slate-500">Our Experts Works with your technologies <span class="animate-pulse text-red-500">&hearts;</span></h2>
      <div class="flex gap-8 md:gap-20 items-center justify-center mt-10 flex-wrap">
        <Icon class="size-8 md:size-12 text-gray-400 hover:text-blue-600 transition-all duration-500 hover:scale-125" name="simple-icons:flutter" />
        <Icon class="size-8 md:size-12 text-gray-400 hover:text-green-600 transition-all duration-500 hover:scale-125" name="simple-icons:nestjs" />
        <Icon class="size-8 md:size-12 text-gray-400 hover:text-orange-500 transition-all duration-500 hover:scale-125" name="simple-icons:swift" />
        <Icon class="size-8 md:size-12 text-gray-400 hover:text-purple-600 transition-all duration-500 hover:scale-125" name="simple-icons:kotlin" />
        <Icon class="size-8 md:size-16 text-gray-400 hover:text-blue-500 transition-all duration-500 hover:scale-125" name="simple-icons:react" />
      </div>
    </div>

    <!-- Main CTA -->
    <div class="text-center mb-16 animate-slide-in-up">
      <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-blue-100 to-purple-100 rounded-2xl mb-6 animate-pulse-gentle">
        <Icon class="w-10 h-10 text-blue-600" name="bx:rocket" />
      </div>

      <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-4 animate-slide-in-up-delayed">
        Ready to Start Your Project?
      </h2>
      
      <p class="text-xl text-gray-600 max-w-2xl mx-auto mb-8 animate-slide-in-up-more-delayed">
        Let's build something amazing together.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center animate-slide-in-up-final">
        <a 
          href="/contact" 
          class="group inline-flex items-center justify-center px-8 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg"
        >
          <Icon class="w-5 h-5 mr-2 group-hover:animate-bounce" name="bx:envelope" />
          Get Started
          <Icon class="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform duration-300" name="bx:right-arrow-alt" />
        </a>
        <a 
          href="/about" 
          class="group inline-flex items-center justify-center px-8 py-3 border border-gray-300 hover:border-gray-400 text-gray-700 font-medium rounded-lg transition-all duration-300 hover:scale-105 hover:bg-gray-50"
        >
          <Icon class="w-5 h-5 mr-2 group-hover:rotate-12 transition-transform duration-300" name="bx:info-circle" />
          Learn More
        </a>
      </div>
    </div>

    <!-- Enhanced Contact Info -->
    <div class="text-center animate-fade-in-final">
      <div class="inline-flex items-center justify-center space-x-3 p-4 bg-white rounded-full shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-105">
        <Icon class="w-5 h-5 text-blue-600" name="bx:envelope" />
        <a href="mailto:info@masterfabric.co" class="text-gray-700 hover:text-blue-600 transition-colors font-medium">
          info@masterfabric.co
        </a>
      </div>
    </div>

  </div>
</section>

<style>
  /* Floating animations */
  @keyframes float {
    0%, 100% { 
      transform: translateY(0px) rotate(0deg); 
    }
    50% { 
      transform: translateY(-20px) rotate(5deg); 
    }
  }

  @keyframes float-delayed {
    0%, 100% { 
      transform: translateY(0px) rotate(0deg); 
    }
    50% { 
      transform: translateY(-15px) rotate(-3deg); 
    }
  }

  @keyframes float-slow {
    0%, 100% { 
      transform: translateY(0px); 
    }
    50% { 
      transform: translateY(-10px); 
    }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slide-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes pulse-gentle {
    0%, 100% { 
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.2);
    }
    50% { 
      transform: scale(1.05);
      box-shadow: 0 0 0 10px rgba(59, 130, 246, 0);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float-delayed 8s ease-in-out infinite;
  }

  .animate-float-slow {
    animation: float-slow 10s ease-in-out infinite;
  }

  .animate-pulse-gentle {
    animation: pulse-gentle 3s ease-in-out infinite;
  }

  .animate-fade-in-up {
    animation: fade-in-up 1s ease-out;
  }

  .animate-slide-in-up {
    animation: slide-in-up 0.8s ease-out 0.2s both;
  }

  .animate-slide-in-up-delayed {
    animation: slide-in-up 0.8s ease-out 0.4s both;
  }

  .animate-slide-in-up-more-delayed {
    animation: slide-in-up 0.8s ease-out 0.6s both;
  }

  .animate-slide-in-up-final {
    animation: slide-in-up 0.8s ease-out 0.8s both;
  }

  .animate-fade-in-final {
    animation: fade-in-up 1s ease-out 1s both;
  }

  @media (max-width: 640px) {
    .absolute.top-20,
    .absolute.bottom-20,
    .absolute.top-1\\/2,
    .absolute.top-32,
    .absolute.bottom-32,
    .absolute.top-1\\/3,
    .absolute.bottom-1\\/3,
    .absolute.top-1\\/4,
    .absolute.bottom-1\\/4 {
      display: none;
    }
  }
</style>
